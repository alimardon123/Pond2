pyiceberg[sql-sqlite,pyarrow]>=0.11.0
pyarrow>=15.0
# numpy: fast path for physical_structures encoding (bitpack vectorized
# predicate eval). Without it the pure-Python fallback is 50-100x slower —
# the bitpack benchmark ran 291s in CI (97% of its 300s subprocess timeout,
# flaking over the line on slower runners).
numpy>=1.26
duckdb>=1.0
zstandard>=0.23
cryptography>=42.0
pytest>=8.0
boto3>=1.34
moto[server]>=5.0
